---
import { env } from 'cloudflare:workers';
import AdminLayout from '../../../layouts/AdminLayout.astro';
import { getAllProductTypes } from '../../../lib/db';

const productTypes = await getAllProductTypes(env.DB);
---

<AdminLayout title="Product types">
	<a href="/admin/product-types/new" class="mb-4 inline-block bg-blue px-5 py-2.5 text-sm text-white">
		New product type
	</a>

	{productTypes.length === 0 ? (
		<p class="text-gray">No product types yet.</p>
	) : (
		<div class="overflow-x-auto bg-white border border-black/10">
			<table class="w-full text-left text-sm">
				<thead>
					<tr class="border-b border-black/10 text-gray">
						<th class="px-4 py-3 font-medium">Category</th>
						<th class="px-4 py-3 font-medium">Name</th>
						<th class="hidden px-4 py-3 font-medium sm:table-cell">Slug</th>
						<th class="px-4 py-3 font-medium"></th>
					</tr>
				</thead>
				<tbody>
					{productTypes.map((productType) => (
						<tr class="border-b border-black/5 last:border-0">
							<td class="px-4 py-3 text-gray">{productType.category_name}</td>
							<td class="px-4 py-3 text-ink">{productType.name}</td>
							<td class="hidden px-4 py-3 text-gray sm:table-cell">{productType.slug}</td>
							<td class="px-4 py-3 text-right">
								<a href={`/admin/product-types/${productType.id}/edit`} class="text-blue underline">
									Edit
								</a>
							</td>
						</tr>
					))}
				</tbody>
			</table>
		</div>
	)}
</AdminLayout>
