---
import { env } from 'cloudflare:workers';
import AdminLayout from '../../../layouts/AdminLayout.astro';
import { getAllPetTypes } from '../../../lib/db';

const petTypes = await getAllPetTypes(env.DB);
---

<AdminLayout title="Pet types">
	<a href="/admin/pet-types/new" class="mb-4 inline-block bg-blue px-5 py-2.5 text-sm text-white">
		New pet type
	</a>

	{petTypes.length === 0 ? (
		<p class="text-gray">No pet types yet.</p>
	) : (
		<div class="overflow-x-auto bg-white border border-black/10">
			<table class="w-full text-left text-sm">
				<thead>
					<tr class="border-b border-black/10 text-gray">
						<th class="px-4 py-3 font-medium">Category</th>
						<th class="px-4 py-3 font-medium">Name</th>
						<th class="hidden px-4 py-3 font-medium sm:table-cell">Slug</th>
						<th class="px-4 py-3 font-medium"></th>
					</tr>
				</thead>
				<tbody>
					{petTypes.map((petType) => (
						<tr class="border-b border-black/5 last:border-0">
							<td class="px-4 py-3 text-gray">{petType.category_name}</td>
							<td class="px-4 py-3 text-ink">{petType.name}</td>
							<td class="hidden px-4 py-3 text-gray sm:table-cell">{petType.slug}</td>
							<td class="px-4 py-3 text-right">
								<a href={`/admin/pet-types/${petType.id}/edit`} class="text-blue underline">
									Edit
								</a>
							</td>
						</tr>
					))}
				</tbody>
			</table>
		</div>
	)}
</AdminLayout>
